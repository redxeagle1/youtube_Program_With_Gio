## intro
it can be refered as  a hardware or a sotware 
### hardware part
on the hardware part it's just a computer that store the web server software, source code abd other stuff 
### software part
on the software part 
- a web server can process incomming request as we menion earlier using different protocols and ==typically it's the HTTP== which is the protocole used by the browser to view web pages
- a web server can host a single or multiple websites on the same server using the same resources
- this is done by something called virtaul host where the single web server is able to allocate annd share resources accross mutiple web sites 
### how a web server works it works
1. the browser will request the file for a web server. 
2. then the web server will process this request and responde accordinly
3. if the web server couldn't find it it will return a 404 error

you can see this in action in the network section of the dev tools of the web

